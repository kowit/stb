<div class="card">
  <div class="card-content">
    <div class="columns">
      <div class="column auto">
        <strong class="remove-item">
          <%#= link_to "", line_item, method: :delete, data: {confirm: "Are you sure?"}, class: "delete is-large is-danger" %>
          <%#= link_to line_item, method: :delete, data: {confirm: "Are you sure?"}, class: "delete is-large is-danger" do %>
            <%#= fa_icon "times-circle" %>
            <%# end %>
        </strong>
      </div>
      <div class="column is-one-fifths">
        <figure class="is-128x128 image">
          <%= image_tag(line_item.item.image_url(:thumb)) %>
        </figure>
        <!-- <strong>Amount: <%#= line_item.quantity %></strong> -->
      </div>

      <div class="column is-half">
        <strong>
          <h1 class="title">
            <%= line_item.item.name %>
          </h1>
        </strong>
        <%#= sanitize(line_item.item.description) %>
        <div>
          Size: <%= line_item.size %>
        </div>
        <div>
          Flavor: <%= line_item.flavor %>
        </div>
        <div>
          Add-in: <%= line_item.addins %>
        </div>
        <div>
          Espresso Shots: <%= line_item.espresso_shots %>
        </div>
      </div>
      <div class="column is-one-quarter">
        <h1 class="subtitle">
          <strong>
            <%= number_to_currency(line_item.item.price) %>
          </strong>
        </h1>
      </div>

      <div class="column is-one-quarter">
        <%= link_to "", line_item, method: :delete, data: {confirm: "Are you sure?"}, class: "button is-medium delete is-large is-danger" %>
      </div>
      <!--
        <strong class="f4">
        <%#= number_to_currency(line_item.total_price) %>
        </strong>
      -->
    </div>


    <% if line_item.item_type == "Cold Drink" %>
      <%= form_for line_item do |form| %>
        <div class="select">
          <%= form.select :size, options_for_select(["None", "Small", "Medium", "Large"]), class: "select-size-options" %>
        </div>
        <div class="select">
          <%= form.select :flavor, options_for_select(["None", "Simple Syrup", "Hazelnut Syrup", "Vanilla Syrup", "Toffee Nut Syrup"]), class: "select-size-options" %>
        </div>
        <div class="select">
          <%= form.select :addins, options_for_select(["None", "Splash of 2% Milk", "Splash of 1% Milk", "Splash of Almondmilk", "Splash of Soymilk"]), class: "select-size-options" %>
        </div>
        <div class="select">
          <%= form.select :espresso_shots, options_for_select([0, 1, 2, 3, 4]), class: "select-size-options" %>
        </div>
        <%#= form.number_field(:espresso_shots, in: 0.0..5.0, step: 1.0, class: "espresso-num-field", placeholder: "0") %>
        <%= form.submit "Confirm Options", class: "button" %>
      <% end %>
    <% end %>

    <!--
    <%# if line_item.item_type == "Cold Drink" %>
      <div class="columns">
        <div class="column is-one-quarter">
          <div class="select-drink-size">
            <label class="label">Sizes</label>
            <%#= f.select :size, options_for_select(["None", "Small", "Medium", "Large"]), class: "select-size-options" %>
            <%#= select_tag(line_item.size, options_for_select(["None", "Small", "Medium", "Large"]), class: "select-size-options") %>
          </div>
        </div>
        <div class="column is-one-quarter">
          <div class="flavor-field-container">
            <label class="label">Flavors</label>
            <%#= select_tag(line_item.flavor, options_for_select(["None", "Simple Syrup", "Hazelnut Syrup", "Vanilla Syrup", "Toffee Nut Syrup"]), class: "select-size-options")%>
          </div>
        </div>
        <div class="column is-one-quarter">
          <div class="addins-field-container">
            <label class="label">Add-ins</label>
            <%#= select_tag(line_item.addins, options_for_select(["None", "Splash of 2% Milk", "Splash of 1% Milk", "Splash of Almondmilk", "Splash of Soymilk"]), class: "select-size-options")%>
          </div>
        </div>
        <div class="column is-one-quarter">
          <label class="label">Espresso Shots</label>
          <div class="espresso-shot-options">
            <%#= number_field_tag(line_item.espresso_shots, :quantity, in: 0.0..5.0, step: 1.0, class: "espresso-num-field", placeholder: "0") %>
          </div>
        </div>
      </div>
    <%# end %>
    -->
  </div>
</div>
